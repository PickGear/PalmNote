<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="6dp"
    android:paddingHorizontal="4dp"
    android:background="@drawable/widget_item_bg"
    android:layout_marginBottom="2dp">

    <TextView
        android:id="@+id/widget_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textSize="14sp"
        android:textColor="@color/widget_title_color"
        android:fontFamily="sans-serif-medium"
        android:maxLines="1"
        android:ellipsize="end" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginStart="12dp"
        android:background="@drawable/widget_days_bg"
        android:paddingHorizontal="10dp"
        android:paddingVertical="3dp">

        <TextView
            android:id="@+id/widget_item_days"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@color/widget_accent_blue"
            android:fontFamily="sans-serif-medium" />

        <TextView
            android:id="@+id/widget_item_unit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="天"
            android:textSize="12sp"
            android:textColor="@color/widget_label_color"
            android:fontFamily="sans-serif"
            android:layout_marginStart="2dp" />
    </LinearLayout>

</LinearLayout>